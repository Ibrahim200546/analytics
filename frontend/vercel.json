{
  "framework": "nextjs",
  "buildCommand": "yarn build",
  "installCommand": "yarn install --frozen-lockfile",
  "devCommand": "yarn dev",
  "cleanUrls": true,
  "crons": [
    {
      "path": "/api/news",
      "schedule": "0 6 * * 1"
    }
  ]
}
